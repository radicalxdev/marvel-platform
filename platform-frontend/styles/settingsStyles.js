const settingsStyles = {
  mainGridProps: {
    container: true,
    width: '100%',
    minHeight: '100%',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'center',
    borderRadius: '24px',
    overflow: 'scroll',
  },
};

export default settingsStyles;
