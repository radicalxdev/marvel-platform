const styles = {
  mainGridProps: {
    item: true,
    container: true,
    justifyContent: 'center',
    alignItems: 'center',
    rowGap: 6,
    bgcolor: (theme) => theme.palette.Dark_Colors.Dark[1],
    borderRadius: '24px',
    overflow: 'hidden',
  },
};

export default styles;
